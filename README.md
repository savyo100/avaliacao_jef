# avaliacao_jef

Projeto em TypeScript / Node.js — backend

## 📄 Sobre o Projeto

Este repositório contém o código-fonte de uma aplicação desenvolvida como avaliação técnica.  
Ele foi estruturado para demonstrar organização de código, uso de TypeScript, configuração de dependências via `package.json`, e uso de ORM/banco (via pasta `prisma`) para persistência de dados.  

### Principais características

- Código escrito em TypeScript.  
- Uso de configuração via `tsconfig.json`.  
- Gerenciamento de dependências por `package.json` e `package-lock.json`.  
- Estrutura organizada de pastas: `src/`, `.vscode`, `prisma/`, etc.  
- (Se aplicável) Utilização de ORM/gerenciamento de banco com `prisma`.  

## 🚀 Como executar / rodar o projeto localmente

> ⚠️ Dependendo da configuração, pode haver variáveis de ambiente ou configurações adicionais — adapte conforme o seu ambiente.

1. Clone o repositório  
   ```bash
   git clone https://github.com/savyo100/avaliacao_jef.git
2. inicie
   npm i
